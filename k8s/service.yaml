apiVersion: v1
kind: Service

metadata:
  name: nginx-devops-service
  namespace: devops-projects

spec:
  type: NodePort

  selector:
    app: nginx-devops

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30082
